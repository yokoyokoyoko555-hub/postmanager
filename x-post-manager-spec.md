# トレカショップ向け SNS投稿管理システム 仕様書

対象: トレーディングカードショップの集客・通販エンゲージメント強化のための、
X（旧Twitter）／Instagram 複数アカウント運用ツール。
Claude Code での実装を前提とした要件・アーキテクチャ定義。

---

## 1. 目的

- 複数のX/Instagramアカウント（ブルーバッジ取得済み）の投稿を一元管理
- 投稿の下書き作成・テンプレート化・予約投稿・AIによる文案生成をワンストップで行う
- 最終的な狙いは「来店集客」と「通販（EC）への送客」

## 2. 機能要件

### 2.1 マルチアカウント管理
- X / Instagram それぞれ複数アカウントを登録・切替できる
- アカウントごとに OAuth トークンを個別に保持
- アカウントごとの投稿実績（成功/失敗）を記録

### 2.2 下書き管理
- テキスト（＋画像）を保存し、随時編集可能
- ステータス: `draft`（下書き）→ `scheduled`（予約済み）→ `posted`（投稿済み）→ `failed`（失敗）
- 画像添付（複数枚、X/Instagramそれぞれの上限に対応）

### 2.3 テンプレート機能
- よく使う投稿の型を登録（例: 新入荷速報、セール告知、開封速報）
- `{商品名}` `{URL}` のようなプレースホルダー対応
- テンプレートから下書きを生成

### 2.4 予約投稿
- 日時指定で自動投稿するジョブキュー
- アカウントごと・プラットフォームごとに独立したスケジュール
- 予約の変更・取消
- 実行失敗時のリトライとエラー通知

### 2.5 AI下書き生成
- 入力: 商品/セール情報、トーン（煽り系/情報系/セール告知系/開封速報）、対象アカウント
- 出力: 複数パターンの文案（Claude API 使用）
- 生成結果をそのまま下書きとして保存可能
- プロンプトはショップ業態（トレカ・入荷/セール/通販誘導）に特化させる

### 2.6 投稿実行（本番連携）
- 手動投稿（今すぐ投稿）
- 予約投稿の自動実行（バックエンドのスケジューラーがAPIを叩く）
- 引用ポスト・リポストにも対応（X）

### 2.7 デイリーAIレポート
- 連携済みアカウントのエンゲージメント指標を毎日自動収集
  - 投稿単位: いいね数、リポスト/リプライ数、インプレッション（表示回数）、保存数（Instagram）
  - アカウント単位: フォロワー増減、プロフィール閲覧数（Instagram）、リーチ
- 毎日 **午前11時** に自動実行し、前日分のレポートをAIが生成してダッシュボードに表示
- レポートは3部構成: **前日の振り返り / 改善点 / ネクストアクション**（アカウント間の比較は行わない）
- レポートは管理画面のダッシュボードに常時表示（タブ切替不要、開けばすぐ見える状態）
- 過去のレポートは「レポート履歴」タブから日付ごとに閲覧可能

---

## 3. アーキテクチャ概要

```
[管理画面 (Web UI)]
      │  操作(下書き作成/予約/テンプレ/AI生成/ダッシュボード)
      ▼
[バックエンドAPI]
   ├─ 認証/アカウント管理 (OAuth token store)
   ├─ 下書き/テンプレートCRUD
   ├─ スケジューラー (cron / queue worker)
   ├─ X API クライアント
   ├─ Instagram Graph API クライアント
   ├─ アナリティクス収集ジョブ (毎日11:00に実行)
   ├─ AI生成 (Claude API) ── 下書き文案 & デイリーレポート
   └─ 通知 (Slack/メール等、任意)
      │
      ▼
[DB] accounts / drafts / templates / scheduled_posts / post_logs
    / post_metrics / account_metrics / daily_reports
```

デイリーレポート生成の流れ:
```
毎日 11:00 (cron)
  → 各アカウントの前日投稿の指標をX/Instagram APIから取得
  → post_metrics / account_metrics に保存
  → 前日分の投稿・指標データをClaude APIに渡す
  → 「前日の振り返り／改善点／ネクストアクション」の3部構成でレポート生成
  → daily_reports に保存 → ダッシュボードに表示
```

### 推奨構成（例・自由に置き換え可）
- バックエンド: Node.js (Express/Fastify) または Python (FastAPI)
- DB: PostgreSQL または SQLite（小規模なら十分）
- スケジューラー: node-cron / BullMQ（Redis併用）、または Python なら Celery + Redis
- フロントエンド: 既存プロトタイプ（React）をベースに、実データAPIに接続

---

## 4. データモデル（案）

```
accounts
  id, platform(x|instagram), display_name, handle,
  oauth_access_token, oauth_refresh_token, token_expires_at,
  ig_business_account_id (Instagramのみ), created_at

templates
  id, title, body, created_at

drafts
  id, account_id, platform, text, media_urls[],
  status(draft|scheduled|posted|failed),
  scheduled_at, posted_at, source(manual|ai), template_id,
  quote_target_id (引用ポスト時), created_at

post_logs
  id, draft_id, platform_post_id, status, error_message, executed_at

post_metrics
  id, account_id, platform, platform_post_id,
  likes, reposts, replies, impressions, saves(IGのみ),
  captured_at

account_metrics
  id, account_id, platform, followers_count,
  profile_views(IGのみ), reach, captured_at

daily_reports
  id, report_date,
  review_text(前日の振り返り),
  improvements_text(改善点),
  next_actions_text(ネクストアクション),
  created_at
```

---

## 5. X API 連携仕様

### 5.1 認証
- OAuth 2.0（PKCE, Authorization Code Flow）
- アカウントごとに Developer App のアクセストークン／リフレッシュトークンを保存
- リフレッシュトークンの自動更新処理が必要

### 5.2 主要エンドポイント

| 用途 | エンドポイント |
|---|---|
| 通常投稿作成 | `POST /2/tweets` |
| 引用ポスト作成 | `POST /2/tweets`（`quote_tweet_id` を指定） |
| リポスト | `POST /2/users/{id}/retweets` |
| 画像アップロード | `POST /1.1/media/upload.json`（v1.1のまま。v2側で `media_ids` に紐付け） |
| 自分の投稿取得 | `GET /2/users/{id}/tweets` |
| 投稿の指標取得 | `GET /2/users/{id}/tweets`（`tweet.fields=public_metrics` を指定していいね/リポスト/リプライ/インプレッション数を取得） |
| フォロワー数等 | `GET /2/users/{id}`（`user.fields=public_metrics`） |

指標取得は自分自身のデータへのアクセスなので Owned Reads 扱いになり、通常の読み取りより低コストで運用できる（詳細は Developer Console で確認）。

### 5.3 実装上の注意
- 画像投稿はv1.1のメディアアップロード→v2の投稿作成という2段階
- 通販リンクは本文に直書きせず、固定ポストやリプライ欄に置く設計にすると運用コストと表示上のノイズを抑えやすい
- レート制限（15分単位のウィンドウ）を考慮したリトライ・キューイング設計にする
- クレジット残高チェック／エラーハンドリング（残高不足時のアラート）を組み込む

---

## 6. Instagram（Meta Graph API）連携仕様

### 6.1 事前準備（実装前にショップ側で対応が必要）
1. 無料の Facebook ページを作成（個人アカウントの日常利用は不要、業務用ページを1つ作るだけ）
2. Instagramアカウントを「ビジネス」または「クリエイター」アカウントに切替
3. InstagramアカウントをそのFacebookページにリンク
4. Meta for Developers でアプリを作成し、Instagram Graph API を追加

### 6.2 投稿フロー（2段階）
1. メディアコンテナ作成: `POST /{ig-user-id}/media`（画像は公開URLが必要。JPEG推奨、アスペクト比 4:5〜1.91:1）
2. 公開実行: `POST /{ig-user-id}/media_publish`

### 6.3 制約
- ローカル画像はそのまま渡せない → S3等のクラウドストレージにアップロードして公開URL化する処理が必要
- レートリミット: 1時間あたり最大200 APIコール（目安、変動あり）
- アクセストークンの長期化・無期限化と定期的な更新運用が必要

### 6.4 アナリティクス（インサイト）取得

| 用途 | エンドポイント |
|---|---|
| 投稿単位の指標 | `GET /{ig-media-id}/insights`（いいね・コメント・保存数・リーチ等） |
| アカウント単位の指標 | `GET /{ig-user-id}/insights`（プロフィール閲覧数・リーチ・フォロワー推移等） |

インサイト系エンドポイントはプロアカウント（ビジネス/クリエイター）限定。取得できる指標項目はメディアタイプ（画像/リール等）によって異なるため、実装時にドキュメントで最新の対応指標を確認する。

---

## 7. デイリーAIレポートの実装方針

### 7.1 収集バッチ
- 毎日 **11:00** に、全アカウント分の前日投稿指標・アカウント指標をAPIから取得し `post_metrics` / `account_metrics` に保存
- インプレッション等はプラットフォーム側の反映に時間差があるため、前日確定分を対象にする

### 7.2 レポート生成
- 集計データ（前日の投稿一覧＋指標）をClaude APIに渡し、日本語でレポート文章を生成
- 出力は必ず次の3部構成にする（アカウント同士の比較・ランキングは行わない）
  1. **前日の振り返り**: 投稿内容・反応の要約
  2. **改善点**: 反応が薄かった投稿の傾向や気づき
  3. **ネクストアクション**: 今日以降の投稿で試すべき具体的なアクション
- 生成したレポートは `daily_reports` に保存
- ダッシュボード（トップ画面）に常時表示。過去分は「レポート履歴」タブで日付を選んで閲覧

### 7.3 実装上の注意
- 収集対象は「前日投稿分」に絞り、指標APIの呼び出しを最小限にする
- レポート生成失敗時は前回分を表示したまま、エラーのみ管理画面に表示する
- 複数アカウントがある場合も、レポートは横比較せず「全体としてどうだったか」をまとめる形にする

---

## 8. AI生成の実装方針

- Claude API（`claude-sonnet-4-6` 等）にプロンプトを送信し、JSON形式で複数パターンを受け取る
- プロンプトに含める要素:
  - 店舗名・アカウント特性
  - 投稿の元情報（入荷内容・セール内容など）
  - トーン指定
  - 文字数条件（X: 280字以内、Instagramはキャプションなので長文可）
  - 通販/来店への送客を意識した一文を必ず含める
- 生成結果はそのまま下書きに保存し、人がレビューしてから予約/投稿する運用を推奨（全自動投稿は炎上・誤投稿リスクがあるため）

---

## 9. セキュリティ・運用

- OAuthトークン・APIキーは環境変数 or シークレットマネージャーで管理（コードに直書きしない）
- 複数アカウント運用のため、トークンごとのアクセス範囲を最小化
- 投稿前に文字数・NGワード等のバリデーションを入れる
- 予約実行の失敗時に管理画面へアラート表示 or 通知（Slack/メール等）

## 10. 未確定・要検討事項

- スケジューラーのホスティング先（自前サーバー / Vercel Cron / AWS Lambda等）
- 画像ストレージの選定（S3 / Cloudflare R2 等）
- 承認フロー（AI生成→人のレビュー→予約、を必須にするかどうか）
- 投稿失敗時の自動リトライ回数・間隔
- 日次レポートの配信先（ダッシュボードのみ／Slack・メールも含めるか）
- 指標の保存期間（無期限に貯めるか、一定期間で集約・間引くか）

---

## 11. 参考: 既存プロトタイプ

UIプロトタイプ（React, ダミーデータ）を作成済み。データモデルは本仕様のdrafts/templatesに近い形で設計されているため、実データAPIに接続する形で流用可能。
